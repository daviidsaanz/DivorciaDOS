using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerOnTrigger : MonoBehaviour
{
    /*[SerializeField] private MovingGroundTrigger movingGroundTrigger;  // Referencia al script de MovingGroundTrigger

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Player1"))
        {
            Debug.Log("Player 1 ha ENTRADO en el punto.");
            movingGroundTrigger.player1OnTrigger = true;  // Activar cuando Player 1 entra
        }
        else if (other.CompareTag("Player2"))
        {
            Debug.Log("Player 2 ha ENTRADO en el punto.");
            movingGroundTrigger.player2OnTrigger = true;  // Activar cuando Player 2 entra
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Player1"))
        {
            Debug.Log("Player 1 ha salido del punto.");
            movingGroundTrigger.player1OnTrigger = false;  // Desactivar cuando Player 1 sale
        }
        else if (other.CompareTag("Player2"))
        {
            Debug.Log("Player 2 ha salido del punto.");
            movingGroundTrigger.player2OnTrigger = false;  // Desactivar cuando Player 2 sale
        }
    }*/
}
